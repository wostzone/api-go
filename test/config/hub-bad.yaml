# Bad configuration file for tests
# DONT CHANGE THE LINES AS THE TEST EXPECTS AN ERROR ON LINE 11

# Message bus server
messenger:
  #protocol: "" # internal, MQTT, default internal
  #address: "localhost" # hostname or address to listen on
  #certPortMqtt: 33100 # certificate based auth over MQTT/TLS
  #unpwPortWS: 33101 # username password auth over WebSocket/TLS
  # the next line intentionally causes an error 
  -brokenyamlfileonline12
  timeout: 1

#certsFolder: "./certs" # set to activate TLS

# Logging
logging:
  # logFile: "/var/log/wost/hub.log"
  logLevel: "debug" # debug, info, warning, error. Default is warning

# Plugins to start
plugins: # list of plugins to start
  autostart
  - msgbus
