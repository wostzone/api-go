# Bad configuration file for tests

# Message bus server
messenger:
  #certFolder: "./certs" # set to activate TLS
  #protocol: "" # internal, MQTT, default internal
  #address: "localhost" # hostname or address to listen on
  #port: 9678
  # the next line intentionally causes an error 
  -brokenyamlfile
  timeout: 1

# Logging
logging:
  # logFile: "/var/log/wost/hub.log"
  logLevel: "debug" # debug, info, warning, error. Default is warning

# Plugins to start
plugins: # list of plugins to start
  autostart
  - msgbus
